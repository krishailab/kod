export const metadata = {
  title: 'Verify Credential',
  description: 'Verify a KodRish credential or certificate securely.',
}

export default function VerifyLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return children
}
