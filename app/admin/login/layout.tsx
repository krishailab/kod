export const metadata = {
  title: 'Admin Login',
  description: 'Login to the KodRish admin dashboard.',
  robots: 'noindex, nofollow', // Good practice for admin login pages
}

export default function AdminLoginLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return children
}
